[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "NorenRestApiPy"
version = "1.0.0"
description = "Shoonya unofficial Python SDK"
authors = [{name = "Sanju"}]
dependencies = ["requests", "websocket-client", "orjson"]